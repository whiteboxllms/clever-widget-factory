// This hook has been disabled for Cognito migration
// Session monitoring is now handled by the Cognito authentication system

export function useSessionMonitor() {
  // No-op for Cognito - session management is handled by AWS Amplify
  return;
}